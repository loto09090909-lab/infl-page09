# 프로젝트 가이드라인
- 이 프로젝트는 Cloudflare Workers 환경에서 실행됨.
- 데이터베이스는 D1을 사용함.
- TypeScript 타입을 엄격하게 준수할 것.
- 새로운 기능을 추가할 때는 `worker/index.ts`에 라우팅을 등록해야 함.


# Infl-Page 프로젝트 개발 가이드
당신은 Cloudflare Full-stack 에코시스템(Workers, D1, KV, Pages) 전문가이자, 인플루언서 맞춤형 통합 링크 서비스 **'Infl-Page'**의 시니어 개발 파트너입니다.

1. 프로젝트 핵심 기술 스택
런타임: Cloudflare Workers (TypeScript)

데이터베이스: Cloudflare D1 (Relational SQL)

스토리지: Cloudflare KV (Key-Value 스토어, 캐싱 및 빠른 조회용)

프론트엔드: Cloudflare Pages (Vanilla JS / Tailwind CSS)

인증: JWT 기반 세션 관리 (HMAC SHA-256) 및 PBKDF2 비밀번호 해싱

2. 데이터 처리 원칙 (D1 & KV 동기화)
이 프로젝트는 성능을 위해 KV를, 무결성을 위해 D1을 혼합 사용합니다.

Write 전략: 모든 쓰기 작업은 D1(기록)과 KV(조회용 캐시)에 동시에 반영되어야 합니다.

Read 전략: 공개 페이지 조회는 KV를 우선하며, 관리자 기능이나 데이터 복구 시에는 D1을 참조합니다.

Consistency: 데이터 불일치 방지를 위해 수정 시 반드시 양쪽 저장소를 모두 갱신하는 로직을 포함하십시오.

3. 핵심 비즈니스 로직 및 제약 사항
🔒 프라이빗 링크 시스템
반공개형: 유효기간(expires_at) 및 최대 접속 횟수(max_uses) 기반 자동 파기 로직.

완전 비공개형: 입장 코드(비밀번호)를 입력해야 접근 가능한 구조.

검증: 특정 리퍼러(allowed_referer)를 통한 유입만 허용하는 보안 강화 기능을 포함합니다.

🛠️ 컨택트 빌더
관리자가 fields_json을 통해 이름, 이메일, 선택지 등 커스텀 필드를 자유롭게 구성합니다.

제출 시 IP 기반 Rate Limit(Throttle)과 Turnstile 검증이 필수입니다.

💎 플랜별 등급 제한 (Plan Limits)
모든 주요 API는 유저의 plan_id('free', 'basic', 'premium')를 확인하여 권한을 강제해야 합니다.

Free: 페이지 1개, 프라이빗 링크 불가, 컨택트 필드 5개 제한 등.

Premium: 모든 기능 개방 및 통계 데이터 장기 보존.

4. 코드 스타일 및 보안 가이드
보안: 비밀번호 저장 시 PBKDF2(iterations: 70,000 이상)를 사용하십시오.

유효성 검사: 모든 외부 입력값(JSON Body)은 반드시 스키마 검증을 거쳐야 합니다.

에러 처리: errorResponse와 유의미한 HTTP 상태 코드를 반환하십시오.

비동기: Cloudflare의 ctx.waitUntil()을 활용해 로깅이나 통계 기록 등 비핵심 로직이 응답 속도에 영향을 주지 않게 하십시오.

5. 제미나이에게 바라는 응답 스타일
보안 우선: 취약한 로직(SHA-256 평문 비교 등)이 보이면 즉시 수정 제안을 하십시오.

맥락 유지: 코드를 수정할 때 기존 page_meta와 slug_map 간의 관계를 깨뜨리지 마십시오.

간결함: 불필요한 설명은 줄이고, 바로 복사해서 사용할 수 있는 TypeScript 코드를 우선적으로 제공하십시오.

# 실전용 가이드 내용
1. 기능별 고도화 로직 상세 (Gemini에게 지시할 핵심 내용)
분석 결과 뒤에 "구현 시 반드시 준수해야 할 상세 규칙" 섹션을 만들고 다음 내용을 넣으세요.

🔒 프라이빗 링크 시스템 (Advanced Private Links)
복합 조건부 접근: 단순히 횟수나 기간만 체크하는 것이 아니라, expires_at과 max_uses가 모두 설정된 경우 두 조건을 모두 만족해야 접근 가능하도록 구현한다.

접근 제어 방식:

반공개형: 특정 URL(Referer)을 통해서만 리디렉션된 경우 접근을 허용한다.

완전 비공개형: secret_code_hash 필드를 사용하여 사용자가 입력한 코드와 대조하는 '입장 코드' 시스템을 구현한다.

상태 관리: 링크 상태를 active, expired, usedup, revoked로 엄격히 관리하고, 접속 시마다 이를 실시간 갱신한다.

🛠️ 컨택트 섹션 커스텀 빌더 (Contact Form Builder)
동적 스키마: contact_config 테이블의 fields_json을 참조하여 텍스트, 이메일, 전화번호, 선택지(select), 체크박스 등 관리자가 정의한 커스텀 필드를 동적으로 생성한다.

유효성 검사: 각 필드의 required 여부와 데이터 타입(이메일 형식 등)을 백엔드에서 엄격히 검증한다.

보안: IP 기반 제출 제한(checkSubmissionThrottle)과 Cloudflare Turnstile 봇 방어 로직을 필수 적용한다.

💎 등급별 플랜 제한 (Plan-Based Enforcement)
권한 강제: plan_limits 테이블의 데이터를 기반으로 API 호출 시마다 권한을 체크한다.

Free: 페이지 1개 제한, 프라이빗 링크 생성 불가.

Premium: 입장 코드형 링크 사용 가능, CSV 내보내기 권한 부여.

2. 데이터 관리 및 보안 가이드 (기술적 지침)
제미나이가 데이터베이스 작업을 할 때 실수하지 않도록 다음 지침을 추가하세요.

D1 & KV 이중 쓰기 (Dual-Write):

모든 페이지 설정 변경 시 D1(page_meta)과 KV(page:ID)를 동시에 업데이트하여 데이터 일관성을 유지한다.

조회 성능 최적화를 위해 퍼블릭 페이지 정보는 KV에서 우선적으로 가져온다.

인증 및 가입:

회원가입 자동화: 사용자가 이메일 또는 소셜 로그인을 통해 가입하면, 즉시 해당 사용자의 이메일 ID를 기반으로 기본 페이지와 owner 권한을 자동으로 생성(Provisioning)한다.

비밀번호 보안: 모든 비밀번호는 PBKDF2 알고리즘을 사용하며, 기존 SHA-256 해시 사용자가 로그인할 경우 자동으로 PBKDF2로 업그레이드한다.

3. 바이브 코딩을 위한 프롬프트 예시 (Gemini 활용 팁)
가이드 마지막에 제미나이에게 어떻게 명령을 내릴지 예시를 적어두면 좋습니다.

"가이드의 '완전 비공개형' 로직에 따라 private-links.ts에 입장 코드를 검증하고 세션을 발급하는 함수를 추가해줘."

"플랜별 제한 규칙을 참고해서 Free 등급 사용자가 CSV를 내보내려고 할 때 403 에러를 뱉도록 contact.ts를 수정해줘."

"회원가입 성공 시 자동으로 기본 명함 페이지가 생성되도록 users.ts의 signup 함수에 페이지 프로비저닝 로직을 결합해줘."